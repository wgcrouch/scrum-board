{% extends 'itsallagileBootstrapBundle::base.html.twig' %}

{% block body %}
        <div class="span6">
            <div class="page-header">
                <h1>Add a board</h1>
            </div>
            <form action="{{ path('core_boards_add') }}" method="post" class="well" {{ form_enctype(form) }}>
                    {{ form_widget(form) }}
                    <button type="submit" class="btn">Add</button>	
            </form>
        </div>
{% endblock %}
